<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Show</title>
    <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.2/dist/css/bootstrap.min.css" integrity="sha384-T3c6CoIi6uLrA9TneNEoa7RxnatzjcDSCmG1MXxSR1GAsXEV/Dwwykc2MPK8M2HN" crossorigin="anonymous">
</head>
<body>
    <h2 class="offset-2 mt-3 mb-3">Article id: <%= blog.id %></h2>
    <h2 class="offset-2 mt-3 mb-3">Bharat Intern</h2>
    <p class="offset-2 mt-3 mb-3"> <%= blog.createdAt.toLocaleDateString("en-GB")%> </p>
    <form action="/blogs"  class="offset-2 mt-3 mb-3" method="GET" style="display:inline">
        <button type="button submit" class="btn btn-primary" formaction="/blogs">All Articles</button> &nbsp;&nbsp;
        <button type="button submit" class="btn btn-info" formaction="/blogs/<%= blog._id %>/edit">Edit Article</button>
    </form><br/><br/>
        <div class="offset-2 col-8 card">
            <div class="mb-3 card-body">
                <h4>
                    Title:<br/>
                    <small class="text-body-secondary"><%= blog.title %></small>
                  </h4>
            </div>
            <div class="mb-3 card-body">
                <h4>
                    Description:<br/>
                    <small class="text-body-secondary"><%= blog.description %></small>
                  </h4>
            </div>
            <div class="mb-3 card-body">
                <h4>
                    Markdown:<br/>
                    <small class="text-body-secondary"><%= blog.markdown %></small>
                  </h4>
            </div>
        </div>
    
</body>
</html>